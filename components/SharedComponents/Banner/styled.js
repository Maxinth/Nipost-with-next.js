import styled from "styled-components";

const Section = styled.section``;
const BannerImage = styled.img`
  object-fit: contain;
  width: 100%;
  display: block;
`;

export { Section, BannerImage };
