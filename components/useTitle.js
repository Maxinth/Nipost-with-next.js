import React from 'react'
import Router from "next/router";

const useTitle = () => {
    const {router} = Router;
    const { pathname } = router;
    
    return (
        
    )
}

export default useTitle
